# Application
APP_NAME=Industrial AI Safety Backend
APP_VERSION=0.1.0
APP_ENV=dev

# Legacy edge config
EDGE_CONFIG_PATH=config.yaml

# Optional model overrides
# If these are empty, backend will use values from config.yaml
MODEL_PATH=
MODEL_INPUT_SIZE=
MODEL_CLASS_NAMES=
DETECT_CONF_THRESHOLD=
DETECT_IOU_THRESHOLD=

# Database
DATABASE_URL=sqlite:///./storage/app.db
